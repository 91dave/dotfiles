# Copy to /etc/ssh/sshd_config.d/10-podman-wsl.conf (needs root).
# Serves the podman API to the Windows podman client and Podman Desktop over SSH.
ListenAddress 127.0.0.1
PasswordAuthentication no
KbdInteractiveAuthentication no
PubkeyAuthentication yes
